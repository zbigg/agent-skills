description = "Call CARTO Location Data Services — geocoding, reverse geocoding, isolines, routing, OD matrices — synchronously and inline via the CARTO MCP server's lds_* tools, for a handful of inputs needed now in the conversation. Provider is account-configured, never chosen by the agent. Distinct from carto-geocoding and carto-routing-od-analysis, which build CARTO Workflows for whole tables (carto-skills)"

prompt = """
Apply the `carto-location-services` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-location-services/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-location-services/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
